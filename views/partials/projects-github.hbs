<article class="github">
  <ul class="nav-list">
    {{#each this}}
      <li > 
        <div class="row">
          <a class="repo-name" href="{{this.url}}">{{this.name}}</a>
          <a class="btn" href="{{this.forkUrl}}">
            <span class="icon-github-fork"></span>
            <span>{{this.forks}}</span>
          </a>
          <a class="btn star" href="{{this.starUrl}}">
            <span class="icon-star"></span>
            <span>{{this.stars}}</span>
          </a>
        </div>
        <p class="row">{{this.desc}}</p>
      </li>
    {{/each}}
  </ul>
</article>
